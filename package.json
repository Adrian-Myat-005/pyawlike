{
  "name": "pyawlike",
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve .",
    "build": "echo \"window.ENV = { GEMINI_API_KEY: '${GEMINI_API_KEY:-REPLACE_ME}' };\" > config.js"
  },
  "dependencies": {
    "@capacitor/android": "6.0.0",
    "@capacitor/cli": "6.0.0",
    "@capacitor/core": "6.0.0"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
