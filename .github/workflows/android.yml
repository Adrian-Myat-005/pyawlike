name: Final God Mode APK Build
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout @v4

      # --- SETUP ENVIRONMENT ---
      - name: Setup Node.js
        uses: actions/setup-node @v4
        with: { node-version: 20 }
      - name: Setup Java
        uses: actions/setup-java @v4
        with: { distribution: 'zulu', java-version: '17' }

      # --- PREPARE ASSETS ---
      - name: Prepare Web Assets
        run: |
          mkdir www
          rsync -av . www --exclude www --exclude node_modules --exclude .git --exclude .github --exclude android

      # --- INSTALL CAPACITOR ---
      - name: Install Dependencies
        run: |
          if [ ! -f package.json ]; then npm init -y; fi
          # We install specific versions to stay safe, but latest is fine with the fix below
          npm install @capacitor/core@latest @capacitor/cli@latest @capacitor/android@latest

      # --- INIT ANDROID ---
      - name: Initialize Android
        run: |
          npx cap init "PyawLike" "com.pyawlike.app" --web-dir=www
          npx cap add android

      # --- 🔥 THE FIX: FORCE KOTLIN VERSION 🔥 ---
      # This step injects a rule to FORCE the project to use Kotlin 1.9.0
      # This solves the "Duplicate Class" error in your screenshot.
      - name: Inject Resolution Strategy
        run: |
          cd android
          
          # Append the fix to the bottom of build.gradle
          cat <<EOT >> build.gradle
          
          allprojects {
              configurations.all {
                  resolutionStrategy {
                      force 'org.jetbrains.kotlin:kotlin-stdlib:1.9.0'
                      force 'org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.9.0'
                  }
              }
          }
          EOT

      # --- BUILD ---
      - name: Build Debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      # --- UPLOAD ---
      - name: Upload APK
        uses: actions/upload-artifact @v4
        with:
          name: pyawlike-app-perfect
          path: android/app/build/outputs/apk/debug/app-debug.apk
